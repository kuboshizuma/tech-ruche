<h2>近日のイベント一覧</h2>
<% if @feeds.present? %>
  <%= render :partial => "feed", :collection => @feeds, :as => :event %>
<% else %>
  <div class="container">
    <div class="no_event_box">
      <p>現在「近日のイベント」はありません。<br><%= link_to "イベントを登録", new_feed_path %>しましょう！</p>
    </div>
  </div>
<% end %>
